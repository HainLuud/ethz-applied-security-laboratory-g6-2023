services:
  server:
    build:
      context: server
    volumes:
      - ./certs/admin.imovies.ch.pem:/app/data/clients/admin/admin.imovies.ch.pem
      - ./certs:/app/certs
      - ca-data:/app/data
    ports:
      - "${CA_PORT}:8000"
volumes:
  ca-data:
