services:
  server:
    build:
      context: .
    volumes:
      - ./rsyslog.conf:/etc/rsyslog.conf
      - ./certs:/etc/certs
      - log-data:/logs
    ports:
      - 6514:6514
    restart: unless-stopped
volumes:
  log-data:
